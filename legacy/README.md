# Legacy (v1)

The original school-project code, kept for history. It is not maintained and
contains hard-coded local paths. The v2 pipeline in `src/drugsyn/` replaces
all of it. See "What changed since v1" in the top-level README.
